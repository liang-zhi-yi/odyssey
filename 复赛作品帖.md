# Odyssey：AI驱动的个人能力文明建造系统

> 作品链接：【待填写：线上体验地址】
> 演示视频：【待填写：B站/YouTube视频链接】
> 代码仓库：https://github.com/liangzhiyi-lanyy/odyssey

---

## 一、团队介绍

我是凉之逸，山东大学大二学生，本次比赛个人参赛。

Odyssey 的诞生源于一个很朴素的念头：日常学习对于大多数人来说太枯燥了。设定目标容易，坚持下去很难——因为进步是模糊的，反馈是延迟的，过程是孤独的。我一直在想，有没有什么方式，能让人们在朝着目标前进的时候，真正感受到乐趣和成就感？

今年三月，我开始接触 AI 编程，这彻底改变了我实现想法的方式。一个人、一个 IDE、一个 AI 伙伴，就能把脑子里的创意变成可以运行的产品——这种亲手将想法落地的感觉让我非常着迷。在和 TRAE 一轮轮的对话与讨论中，"把学习过程游戏化"这个方向逐渐清晰，最终生长成了 Odyssey——用建造一座文明的方式，来记录和驱动每个人的真实成长。

我性格偏内向，不太擅长言辞，但我喜欢用作品说话。Odyssey 就是我想说的话。

---

## 二、作品简介

### 2.1 是什么

**Odyssey 是一个 Web 端 AI 驱动的个人能力成长系统——它将抽象的学习成长过程映射为一座可建造、可探索的个人文明世界，用"每掌握一项技能就解锁一座建筑"的方式，让真实能力增长变得可见、可感、可追踪。**

它不教课程、不卖知识，它解决的核心问题是：**学了很多东西，却不知道自己真正能做什么；努力了很久，却看不到清晰的成长痕迹。**

---

### 2.2 面向谁

**核心目标用户**：有明确自学目标但缺乏反馈机制的终身学习者，尤其是：

- **编程/AI 自学者**：看完教程想验证真实水平，需要通过项目实践获得能力评估
- **跨领域学习者**：同时学习多个技能（如AI+产品+设计），需要一个统一的能力全景图
- **求职/转行者**：需要一份可视化的、可验证的能力证明，比简历上的"精通XX"更有说服力
- **目标驱动型学习者**：设定了成长目标（如"三个月学会Agent开发"），需要结构化路径和持续正反馈

**典型使用场景**：

| 场景 | 用户行为 | Odyssey 的作用 |
|------|---------|---------------|
| 设定学习目标 | 输入"我想成为AI Agent工程师" | AI生成4-6阶段的文明发展路线，明确每一步要做什么 |
| 学习一个新技能 | 完成路径中的检查点任务 | 交付真实作品（代码/架构/提示词），而非做选择题 |
| 验证学习成果 | 提交作品进行AI评估 | 四维能力评分+结构化反馈，知道哪里强、哪里弱 |
| 获得成就感 | 查看文明世界变化 | 建筑解锁/升级、时代跃迁、成就弹窗，成长有即时反馈 |
| 迷茫不知道下一步 | 向AI导师提问 | 基于用户全量数据给出个性化建议 |
| 追踪长期成长 | 查看成长档案 | 能力护照、徽章墙、技能雷达图，看到自己的进步轨迹 |

---

### 2.3 完整功能与用户路径

#### 用户完整使用路径（从注册到成长闭环）

```
注册/登录 → 开场引导 → 设定目标/选择路径 → 接受任务 → 提交作品
    ↓                                            ↑
查看文明世界 ← 建筑解锁/升级 ← 通知反馈 ← AI评估结果 ← 等待评估
    ↓
与AI导师对话 → 调整学习方向 → 回到任务继续成长
    ↓
（长期）积累凭证/徽章 → 文明时代跃迁 → 解锁复合建筑 → 拓展能力疆域
```

#### 功能全景图

**【此处建议插入产品功能架构截图：展示首页仪表盘整体布局】**

Odyssey 的功能分为六大核心模块，覆盖完整成长链路：

##### ① 文明疆域——你的能力可视化世界

用户的能力状态以一座文明城市的形式呈现，这是整个系统的"面子"：

- **文明总览**：文明名称、文明等级、当前时代、已解锁建筑数、文明坐标
- **疆域地图**：左侧水墨风格探索地图（已探索领地+未知迷雾大陆Terra Incognita），右侧文明档案碑展示建筑节点
- **建筑系统**：56项技能对应约55座独特建筑，每座建筑有10级进化形态：
  - 知识文明：学习营地→木质学堂→乡镇学院→…→文明图书馆
  - AI文明：算法小屋→推理室→AI实验室→Agent工坊→…→智能文明核心
  - 工程文明：手工工坊→铁匠铺→制造车间→…→文明制造核心
  - 共12个文明领域（知识/AI/工程/商业/设计/媒体/科学/语言/健康/金融/数字/社会）
- **复合建筑**：多技能协同达标时解锁高级建筑（如"智能体中心"需要Agent+Workflow Design+Python三项能力）
- **时代跃迁**：荒野时代→农耕时代→学院时代→工业时代→信息时代→AI时代→数字文明时代→未来文明时代
- **建筑详情面板**：点击建筑查看对应技能详情、当前等级、升级条件、关联任务

**【此处建议插入文明地图页面截图：展示水墨地图+建筑节点+选中建筑的详情面板】**

##### ② 任务与评估——用真实作品证明能力

这是整个系统的核心引擎，成长的唯一来源：

- **任务（Quest）系统**：
  - 4种类型：知识掌握（KNOWLEDGE）/ 应用实践（APPLICATION）/ 项目建造（PROJECT）/ 精通认证（MASTERY）
  - 4种难度：LEVEL_1~LEVEL_4，通过分数线35/40/45/50分
  - 5种交付物：提示词/架构设计/工作流/代码/报告
  - 每个任务关联具体建筑奖励预览
- **作品提交**：支持文本内容、GitHub链接、Demo链接提交，至少一项非空
- **AI 多维评估引擎**（9步流水线）：
  1. 加载提交内容、任务、评分标准（rubric）
  2. 构建评估提示词（嵌入rubric+任务类型引导）
  3. 多次LLM调用取中位数（一致性校验，避免偶然偏差）
  4. 四维评分：知识(K)×0.2 + 推理(R)×0.25 + 应用(A)×0.35 + 创造(C)×0.2
  5. 每维度输出：分数+评分理由+3项优势+3项不足+3个改进建议
  6. 整体评估：总结+最大优势+最需提升领域+下一步行动建议
  7. 加权平滑更新技能分数（new = old×0.8 + assessed×0.2）
  8. 检查并颁发凭证、徽章
  9. 同步建筑状态（解锁/升级/复合建筑检查/时代跃迁/推送通知）

**【此处建议插入AI评估结果页面截图：展示四维分数卡片+详细反馈+Before/After对比】**

##### ③ AI学习路径——从目标到行动的结构化路线

- **AI路径生成**：输入成长目标（如"成为AI Agent工程师"），LLM两阶段生成：
  - 第一阶段：生成4-6个里程碑×2-5个检查点（共8-25个成长节点），包含阶段描述、目标技能、关联建筑
  - 第二阶段：为每个检查点并发生成2-4个实战任务
- **路径视图**：纵向成长路线图（左侧圆形节点+细线脊柱+右侧内容区），当前阶段高亮，未解锁阶段弱化
- **进度追踪**：细线成长轨迹（暖金渐变+当前进度标记点），完成率可视化
- **路径分析记录**：展示当前阶段、完成预计、推荐下一步行动
- **建筑关联**：路径底部展示关联建筑节点，与路线图视觉统一

**【此处建议插入学习路径详情页截图：展示成长路线图+节点展开+进度轨迹】**

##### ④ AI文明导师——随身成长顾问

常驻右侧边栏（移动端为浮动按钮）的AI对话助手：

- **意图识别**：自动分类用户问题（文明分析/成长规划/建筑顾问/学习顾问/技能查询/世界规则/任务查询/进度查询）
- **全量上下文**：注入用户完整状态（技能分数、建筑状态、当前路径、活跃任务、成长记录）+ 世界规则（所有建筑/技能/时代规则）
- **对话能力**：文明状态分析、下一步建议、建筑解锁指导、学习方向推荐、进度查询、通用对话
- **对话记忆**：20轮历史上下文持久化存储，刷新页面不丢失会话
- **多语言**：根据用户语言偏好自动切换回复语言
- **用户自定义模型**：导师对话使用用户配置的LLM（支持7+供应商）

**【此处建议插入AI导师对话截图：展示侧边栏对话界面，导师回答文明状态分析问题】**

##### ⑤ 仪表盘——成长指挥中心

用户登录后的首页，一屏纵览全局：

- **文明概览Hero区**：当前文明等级、时代、核心指标
- **技能雷达图**：多维度能力分布可视化
- **活跃任务组件**：当前进行中的任务，快速进入提交
- **成长洞察**：近期成长趋势、能力变化
- **路径进度时间线**：当前学习路径的推进状态
- **世界快照**：文明状态快速预览
- **连续成长天数**：Streak激励
- **文明档案**：分类展示各领域建筑卡片

**【此处建议插入首页仪表盘截图：展示整体布局和各组件】**

##### ⑥ 成长档案与成就系统

- **能力护照**：已获得的技能认证凭证
- **徽章墙**：里程碑成就徽章（初次评估、首座建筑、时代跃迁等）
- **成长时间线**：完整历史记录（每次评估分数变化、建筑升级、文明事件）
- **通知中心**：标志性成就实时推送（建筑解锁、升级、时代跃迁、里程碑达成）
- **历史记录**：所有评估和提交的完整存档

##### ⑦ 全局能力

- **LLM自由配置**：用户可设置自己的API Key，支持OpenAI/DeepSeek/阿里百炼/智谱/Moonshot/OpenRouter/自定义接口
- **中英双语**：完整国际化，包括UI文本和AI生成内容
- **移动端适配**：响应式布局，底部导航，触控优化
- **认证与安全**：JWT认证、bcrypt密码哈希、多用户数据隔离、接口参数校验

---

### 2.4 视觉设计

Odyssey 采用独特的**古文明神秘档案**视觉风格，刻意避免AI SaaS常见的蓝紫渐变、霓虹光效、卡片式Dashboard：

- **配色**：宣纸白（#F5F0E6）背景、文明古金（#C8A45D）主色、墨黑（#2B2721）文字
- **质感**：羊皮纸纹理背景、铜质分割线、细线边框、半透明面板
- **图标**：所有建筑使用定制线稿风格图标，无emoji、无通用icon
- **动画**：克制高级——页面入场1-2秒（纹理淡入→边界出现→区域亮起→建筑浮现）、建筑解锁线稿绘制动画、按钮hover金色光线流动
- **交互**：悬停微放大+阴影加深、斜体艺术字体按钮、金色下划线输入框

**【此处建议插入登录页面截图：展示文明认证终端风格+金色下划线输入+启动按钮】**

---

### 2.5 相比初赛Demo的升级

| 维度 | 初赛Demo | 复赛成品 |
|------|---------|---------|
| **文明系统** | 简单的分数→建筑映射，建筑数量少 | 12文明领域、56技能、约55座建筑、复合建筑、8大时代跃迁、水墨疆域地图 |
| **AI评估** | 单维度简单评分 | 四维评估（K/R/A/C）、加权公式、结构化反馈（优势/不足/改进建议）、一致性校验、难度自适应通过线 |
| **学习路径** | 静态预设路径 | LLM动态生成个性化路径，里程碑→检查点→任务三级结构，建筑目标联动，并发任务生成 |
| **AI导师** | 无 | 常驻侧边栏智能体，意图识别，全量用户上下文，20轮对话记忆 |
| **视觉体系** | 常规Web界面 | 古文明神秘档案风格，56+定制建筑图标，羊皮纸/铜质/古金材质系统，克制动画 |
| **LLM支持** | 仅OpenAI | 7+供应商（OpenAI/DeepSeek/百炼/智谱/Moonshot/OpenRouter/自定义），用户可配置，json_schema/json_object双模式适配 |
| **成就反馈** | 无 | 成就通知系统，6类标志性事件实时推送，世界事件日志 |
| **成长档案** | 简单的分数列表 | 能力护照、徽章墙、成长时间线、技能雷达图、进度日志 |
| **国际化** | 仅中文 | 中英双语完整支持，AI内容双语生成 |
| **移动端** | 仅桌面端 | 完整响应式布局，底部导航，触控优化 |
| **数据安全** | 基础功能 | 越权访问修复、输入参数校验、多用户隔离、错误处理完善 |
| **部署** | 本地开发环境 | Docker容器化，前后端分离部署于Railway云平台，CI/CD自动化，生产环境可用 |
| **代码规模** | 基础框架 | 后端约15000行Python、前端约25000行TypeScript、100+UI组件、20个数据库迁移版本、完整测试覆盖 |

---

## 三、作品演示视频

【待填写：此处嵌入演示视频或放置视频链接】

### 视频录制脚本建议（3-5分钟）：

1. **开场（0:00-0:30）**：展示登录页面的文明唤醒动画，进入Odyssey世界
2. **仪表盘总览（0:30-1:00）**：展示首页仪表盘——文明状态、技能雷达、活跃任务、成长路径
3. **文明疆域（1:00-1:30）**：切换到"我的世界"页面，展示文明地图、建筑节点、建筑详情面板
4. **任务提交与AI评估（1:30-2:30）**：选择一个任务，提交作品，展示AI四维评估过程和结果反馈
5. **建筑解锁与成长（2:30-3:00）**：展示评估后技能分数更新、建筑升级、成就通知弹出
6. **AI文明导师（3:00-3:45）**：打开侧边栏，与导师对话，询问文明状态和下一步建议
7. **学习路径生成（3:45-4:15）**：创建新的学习目标，展示AI生成的文明发展路线
8. **收尾（4:15-4:30）**：展示能力护照、徽章墙，总结成长闭环

---

## 四、创作历程

### 4.1 灵感来源：学习为什么这么枯燥？

在自学AI和编程的过程中，我越来越强烈地感受到现有学习工具的痛点：

- **学了很多，但不知道自己会什么**：看完教程、刷完课程，面对真实项目时依然迷茫
- **证书≠能力**：拿到了证书，却无法通过作品集证明真实水平
- **成长没有正反馈**：学习是孤独的，进步是模糊的，缺少像游戏那样清晰可见的成长感
- **工具冰冷，没有情感连接**：SaaS工具都是数据看板和进度条，无法激发持续使用的动力

我想要一个这样的工具：它不教我具体知识（那是课程和教程的事），但它**看见我的成长、证明我的能力、给我探索的乐趣**。

于是有了 Odyssey——把每个人的能力成长变成一个可以建造、可以探索、可以居住的文明世界。

### 4.2 开发过程：和AI一起把创意变成产品

Odyssey 是我第一次独立完成一个完整的全栈产品。从三月接触AI编程到八月提交复赛，五个月的开发过程本身就是一段成长旅程。

项目最开始只是一个模糊的想法——"能不能让学习像建城市一样有趣？"在和TRAE的对话中，这个想法逐渐具象化：从简单的积分系统到文明建筑映射，从单一评分到四维AI评估，从预设路径到AI动态生成，每一轮讨论都让产品更完整一步。

开发过程中印象最深的几个阶段：
- **文明系统的设计**：从"分数对应建筑等级"这个简单想法，到设计出12个文明领域、56项技能、10级建筑进化、8个时代跃迁——这些文明设定不是一开始就想好的，而是在和AI反复讨论中逐步完善的
- **AI评估引擎的打磨**：最初LLM输出的评分非常不稳定，同一个提交两次评分可能差20分。通过加入一致性校验、中位数取值、分数平滑算法，才让评估结果变得可靠
- **视觉风格的反复迭代**：最初的界面是常规的后台管理风格，总觉得和"文明建造"的主题格格不入。后来决定彻底重构为古文明档案风格，从配色、材质、图标到动画全部重新设计，才找到了Odyssey独有的视觉语言
- **部署踩坑**：第一次部署到Railway时遇到了各种问题——Dockerfile构建上下文不对、数据库URL解析错误、seed数据没跑导致页面全空，这些问题都是在TRAE的帮助下一个个排查解决的

功能升级的详细对比见上方 **2.5 相比初赛Demo的升级**。

---

## 五、TRAE 实践过程

### 5.1 TRAE 在开发中的核心作用

Odyssey 从0到1的开发过程中，TRAE 是我的主要开发伙伴，以下是我使用TRAE的真实场景：

#### 全栈代码生成
- 后端API路由、数据库模型、业务逻辑的快速搭建
- 前端组件、页面、样式的生成和迭代
- SQLAlchemy模型、Alembic数据迁移脚本的编写

#### Bug诊断与修复
- 全栈联调时的错误定位（如CORS配置、数据库连接、API代理等问题）
- Railway部署问题排查（Dockerfile构建上下文、环境变量配置、数据库URL解析等）
- TypeScript类型错误、Python异常的快速修复

#### 代码审查与重构
- 代码质量问题识别（如越权访问漏洞、输入校验缺失等）
- 架构优化建议（如LLM客户端抽象、评估引擎模块化等）
- 重构过程中保持功能一致性

#### 测试与验收
- 自动化测试脚本编写
- 全流程用户验收测试
- 边界情况和异常处理验证

#### 视觉设计迭代
- 根据"古文明神秘风格"的设计要求，迭代CSS样式
- 响应式布局适配
- 动画效果实现（如建筑解锁动画、页面过渡效果等）

### 5.2 开发效率提升

作为一名独立开发者，TRAE 让我能够以一人之力完成通常需要3-5人团队的工作量：
- 后端约15000行Python代码
- 前端约25000行TypeScript/TSX代码
- 100+个UI组件
- 20个数据迁移版本
- 完整的测试覆盖
- 生产环境部署上线

整个项目从零到复赛版本，开发周期约五个月（从三月接触AI编程并萌生想法，到八月完成复赛版本）。

### 5.3 Session ID 记录

【待填写：填写TRAE开发过程中的关键Session ID，可在TRAE中查看历史会话获取】

---

## 六、技术方案

### 6.1 整体架构

Odyssey 采用前后端分离的现代Web架构：

```
┌─────────────────────────────────────────────────────────┐
│                     Frontend (Next.js 14)               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │  Pages   │  │Components│  │  Hooks   │  │ Services│ │
│  │(App Router)│  │(100+个)  │  │(SWR数据) │  │(API客户端)│ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
│           TailwindCSS / TypeScript / SWR                │
└──────────────────────────┬──────────────────────────────┘
                           │ HTTPS / REST API (/api/v1/*)
                           ▼
┌─────────────────────────────────────────────────────────┐
│                   Backend (FastAPI)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │   Auth   │  │Assessments│  │  Agent   │  │  Paths  │ │
│  │  (JWT)   │  │ (9步引擎) │  │ (导师)   │  │(AI生成) │ │
│  ├──────────┤  ├──────────┤  ├──────────┤  ├─────────┤ │
│  │  Quests  │  │  World   │  │  Skills  │  │Badges/  │ │
│  │ (任务)   │  │(文明系统) │  │(技能树)  │  │Credentials│ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
│           SQLAlchemy 2.0 / Alembic / Pydantic           │
└──────────────────────────┬──────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│                  PostgreSQL 16                           │
│  用户、技能、任务、评估、建筑、路径、通知、对话历史...    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│              Multi-LLM Provider Layer                    │
│  OpenAI / DeepSeek / 百炼 / 智谱 / Moonshot / OpenRouter │
└─────────────────────────────────────────────────────────┘
```

### 6.2 技术栈选型

| 层级 | 技术选型 | 选型理由 |
|------|---------|---------|
| **前端框架** | Next.js 14 (App Router) | React生态最成熟的全栈框架，内置路由、SSR、API代理，部署方便 |
| **类型系统** | TypeScript 5.x | 类型安全，减少运行时错误，提升开发体验 |
| **样式方案** | TailwindCSS 3.x | 原子化CSS，快速迭代，无需切换文件写样式，便于设计系统统一 |
| **数据请求** | SWR | 轻量的数据请求库，支持缓存、重新验证、乐观更新 |
| **后端框架** | FastAPI | 高性能Python异步框架，自动生成OpenAPI文档，类型提示友好 |
| **ORM** | SQLAlchemy 2.0 | Python最成熟的ORM，支持异步，2.0版本的查询语法更现代 |
| **数据迁移** | Alembic | SQLAlchemy官方迁移工具，版本化管理数据库schema变更 |
| **数据库** | PostgreSQL 16 | 功能强大的开源关系型数据库，支持JSONB（存储灵活的level_names、criteria等） |
| **认证** | JWT (python-jose + bcrypt) | 无状态认证，适合前后端分离架构，bcrypt密码哈希 |
| **LLM SDK** | OpenAI Python SDK | 通过OpenAI兼容接口统一调用多家LLM，减少适配成本 |
| **容器化** | Docker | 一致的开发和生产环境 |
| **云部署** | Railway | 简单易用的PaaS平台，支持Docker部署，自动HTTPS，环境变量管理 |

### 6.3 AI评估引擎（9步流水线）

AI评估是Odyssey的核心技术模块，`backend/app/assessments/engine.py` 实现了完整的9步评估流水线：

```python
def run_assessment(db: Session, assessment_id: str | UUID) -> None:
    # 1. 加载评估记录、提交内容、任务、关联技能
    # 2. 获取该技能的评分标准（rubric）
    # 3. 构建评估提示词（嵌入rubric + 任务类型引导）
    # 4. 加载用户自定义LLM配置（如用户设置了自己的API Key则使用用户配置）
    # 5. 执行一致性评估（多次调用LLM取中位数，确保评分稳定）
    # 6. 将评估结果应用到用户技能分数（加权移动平均平滑更新）
    # 7. 记录成长进度日志
    # 8. 检查并颁发凭证、徽章
    # 9. 同步建筑状态（解锁/升级/复合建筑检查/时代跃迁/创建通知）
```

#### 关键技术细节：

**评分维度与权重**：
- 知识（Knowledge）：权重0.2——对概念和理论的理解
- 推理（Reasoning）：权重0.25——分析问题和逻辑推导能力
- 应用（Application）：权重0.35——将知识应用于实际问题的能力（最高权重，符合"能力优先"原则）
- 创造（Creation）：权重0.2——创新思维和创造性解决方案

**分数平滑更新算法**：
每次评估后，技能分数不是直接替换，而是使用20%加权移动平均：
```python
new_dimension = old_dimension × 0.8 + assessed_dimension × 0.2
```
这避免了单次评估的偶然性导致分数大幅波动，更符合真实能力成长曲线。

**一致性校验协议**（`consistency.py`）：
- 最多进行3次LLM调用
- 验证评分理由长度（≥10字符）
- 检查分数一致性，去除明显异常值
- 最终分数取中位数
- 确保输出的JSON结构完整

### 6.4 多LLM适配层（`core/llm.py`）

Odyssey 实现了一套统一的多LLM调用层，支持7+种LLM供应商：

| 供应商 | Structured Output模式 | 说明 |
|--------|----------------------|------|
| OpenAI | json_schema（严格模式） | 最可靠，API原生强制执行JSON Schema |
| DeepSeek | json_object | 高性能国产模型，API返回合法JSON |
| 阿里百炼（Qwen） | json_object | 通义千问系列 |
| 智谱AI（GLM） | json_object | GLM系列 |
| Moonshot（Kimi） | json_object | 月之暗面 |
| OpenRouter | json_object | 多模型聚合网关（可访问Claude等） |
| 自定义 | json_object | 任何OpenAI兼容接口 |

**响应格式策略分层**：
1. **json_schema（严格模式）**：OpenAI原生支持，API层面保证输出符合JSON Schema，零解析失败
2. **json_object模式**：API返回JSON对象，通过Prompt中注入明确的格式指令引导输出结构，可靠性高
3. **纯文本+正则提取**：兜底方案，从返回文本中用正则提取JSON对象（当前无供应商需要此降级）

每个评估维度输出的结构：
```typescript
interface DimensionScore {
  score: number;           // 0-100整数
  justification: string;   // 评分理由
  strengths: string[];     // 0-3项优势
  weaknesses: string[];    // 0-3项不足
  improvement_actions: string[]; // 0-3个改进建议
}
```

### 6.5 文明进化引擎（`world/upgrade_engine.py`）

文明系统的核心是"真实能力→文明状态"的映射，核心规则：

**建筑等级映射**（`score_to_level`函数）：
技能分数直接决定对应建筑等级，每10分升一级：
```
0-10分 → 1级（LOCKED，未解锁）
11-20分 → 2级
...
90-100分 → 10级（最高等级）
```

**时代跃迁**（`score_to_era`函数）：
累计era_score达到阈值触发时代变迁：
- 荒野时代 → 农耕时代 → 学院时代 → 工业时代 → 信息时代 → AI时代 → 数字文明时代 → 未来文明时代

**复合建筑解锁逻辑**：
复合建筑需要多个关联技能同时达到最低等级要求。例如某复合建筑需要：
- Python ≥ 5级
- Agent ≥ 4级
- Workflow Design ≥ 3级
当用户所有关联技能都满足条件时，复合建筑自动解锁。

**成长事件通知**：
每次评估同步建筑后，系统会检测以下事件并生成通知：
- 新建筑解锁（BUILDING_UNLOCK）
- 建筑升级（BUILDING_UPGRADE）
- 区域解锁（REGION_UNLOCK）
- 复合建筑解锁/升级（COMPOUND_UNLOCK/COMPOUND_UPGRADE）
- 里程碑达成（MILESTONE_REACHED）

### 6.6 AI文明导师（`agent/service.py`）

文明导师是一个具备完整用户上下文的AI对话系统：

**上下文构建层次**：
1. **人设提示词**：导师角色定义、语气指南、回答风格
2. **世界规则上下文**（缓存5分钟）：所有建筑模板、技能列表、时代/等级规则、复合建筑需求、核心成长逻辑
3. **用户实时状态**：用户基本信息、当前文明等级/时代/tier、所有技能分数、所有建筑状态、当前学习路径、活跃任务、最近成长记录、历史对话（最近20轮）
4. **语言指令**：根据用户语言偏好追加语言要求

**意图分类**：
通过关键词匹配初步分类用户意图，帮助LLM更好地组织回答：
- civilization_analysis（文明分析）
- growth_planning（成长规划）
- building_advisor（建筑顾问）
- learning_advisor（学习顾问）
- skill_query（技能查询）
- world_query（世界规则查询）
- quest_query（任务查询）
- progress_query（进度查询）
- general_chat（通用对话）

**对话记忆**：
- 每个用户的对话历史持久化存储在PostgreSQL的`conversation_history`表
- 保留最近20轮对话作为上下文
- localStorage持久化conversation ID，刷新页面不丢失对话
- 支持对话历史搜索和侧边快速导航

### 6.7 AI学习路径生成（`learning_paths/ai_generator.py`）

路径生成采用两阶段LLM调用：

**第一阶段：生成路径结构**
- 输出：path_summary、difficulty、estimated_weeks、civilization_type、milestones列表
- 每个milestone包含title、description、skill_name、building_target、checkpoints列表
- 约束：4-6个milestone、每milestone 2-5个checkpoint、总计8-25个checkpoint
- 最后一个milestone必须是"文明建设"阶段
- 双语输出（中英文）

**第二阶段：为每个checkpoint生成任务**
- 输出：2-4个Quest
- 每个Quest包含title、description、difficulty、quest_type、expected_deliverable
- 任务风格：文明使命式命名（非通用练习题）
- 奖励明确：关联建筑进度和技能成长

**并发任务生成**：
里程碑任务生成采用多线程并发（最多4线程），提升路径生成速度。每个线程使用独立的数据库session，避免并发冲突。

**记忆上下文**（`learning_paths/memory.py`）：
路径生成时会注入用户已有的技能水平、当前建筑状态、已完成路径等上下文，避免生成过于简单或重复的内容。

### 6.8 数据库设计

核心数据表：

| 表名 | 用途 |
|------|------|
| `users` | 用户账号、认证信息 |
| `user_settings` | 用户偏好设置（LLM配置、语言等） |
| `skills` | 技能目录（56项） |
| `user_skills` | 用户技能分数（四维分数+总分+历史记录） |
| `building_templates` | 建筑模板（约55个，定义名称、图标、区域、等级名称） |
| `user_buildings` | 用户建筑实例（等级、状态、建造/升级时间） |
| `compound_building_templates` | 复合建筑模板（需求多技能） |
| `user_compound_buildings` | 用户复合建筑实例 |
| `worlds` | 用户世界（文明等级、时代、tier、资源点数） |
| `world_events` | 世界事件日志（建筑解锁、时代跃迁等历史） |
| `milestone_definitions` | 里程碑定义 |
| `user_milestones` | 用户已达成里程碑 |
| `learning_paths` | 学习路径（AI生成） |
| `milestones` | 路径里程碑 |
| `checkpoints` | 路径检查点 |
| `quests` | 任务模板 |
| `user_quests` | 用户接受/完成的任务 |
| `quest_submissions` | 任务提交内容 |
| `assessments` | AI评估记录（四维分数+反馈） |
| `progress_logs` | 成长进度日志 |
| `credentials` | 凭证定义 |
| `user_credentials` | 用户已获得凭证 |
| `badges` | 徽章定义 |
| `user_badges` | 用户已获得徽章 |
| `notifications` | 通知消息 |
| `conversation_history` | AI导师对话历史 |

**关键设计决策**：
- 使用UUID作为所有表的主键，避免ID枚举攻击
- 建筑模板和用户建筑实例分离（模板是全局数据，用户建筑是 per-user 数据）
- 等级名称（level_names）、技能需求（required_skills）等半结构化数据使用JSONB存储，灵活扩展
- 世界事件（world_events）完整记录文明发展历史，用于时间线展示
- 所有用户数据表都有`user_id`外键，通过查询过滤实现多用户数据隔离

### 6.9 前端架构

#### 组件化设计
100+个UI组件，核心组件分类：

**布局组件**：MainWrapper、PageGlassWrapper、Navbar、MobileBottomNav
**文明主题组件**：CivIcon、CivArchiveTheme、CopperDivider、ParchmentBackground
**仪表盘组件**：DashboardHero、SkillGrowthRadar、ActiveQuestsWidget、GrowthInsightsWidget、WorldSnapshotWidget、StreakWidget
**世界/建筑组件**：WorldMap、RegionMapView、BuildingTile、CompoundBuildingTile、BuildingDetailPanel、TechTreeView、CivilizationOverviewTab、EraTransitionOverlay
**评估组件**：AssessmentResult、AssessmentPolling、AICore、GrowthPhaseIndicator
**路径组件**：LearningPathCard、PathRoadmap、RoadmapNode、PathMilestoneList、PathProgressTimeline、CivilizationPlanner
**任务组件**：QuestCard、QuestDetail、QuestHeader、QuestObjective、SubmissionForm、QuestRewardPanel
**导师组件**：AgentSidebar、AgentMessage、AIMentorPanel、AIMentorCore
**通用组件**：CivilizationBadge、BadgeCard、NotificationBell、NotificationList、AchievementToast、LanguageSwitcher、Loading、ErrorState、EmptyState

#### 数据管理
- 使用SWR进行数据请求和缓存
- 自定义hooks封装：useAuth、useLocale、useAgent
- services层封装所有API调用，组件不直接接触HTTP细节
- TypeScript类型定义完整覆盖所有API响应

#### 样式系统
- 全局CSS变量定义Odyssey色彩体系（oklch色彩空间）
- 工具类：.civ-archive-* 前缀的文明主题样式
- 响应式断点：<768px（移动端）、768-1023px（平板）、≥1024px（桌面）
- 暗色模式支持（通过DarkModeToggle组件）

### 6.10 部署方案

**开发环境**：
- Docker Compose启动本地PostgreSQL
- 后端：uvicorn热重载（localhost:8000）
- 前端：Next.js dev server（localhost:3000）
- 前端通过Next.js rewrites将`/api/*`代理到后端

**生产环境（Railway云部署）**：
- 两个独立Railway服务：
  - **backend服务**：根目录设置为`backend/`，Dockerfile构建，运行`alembic upgrade head && python run_seed.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT`
  - **frontend服务**：根目录设置为`frontend/`，Dockerfile构建，next start启动
- PostgreSQL使用Railway托管PostgreSQL插件
- 环境变量通过Railway Dashboard配置：DATABASE_URL、JWT_SECRET、CORS_ORIGINS、LLM_PROVIDER、LLM_API_KEY等
- 自动HTTPS、自动部署（Git push触发）
- 前后端通过环境变量配置的CORS_ORIGINS实现跨域

**CI/CD**：
- GitHub Actions工作流（backend-ci.yml、frontend-ci.yml）
- 推送代码时自动进行类型检查和构建验证

---

## 七、写在最后

Odyssey 是我对"学习应该是什么样子"这个问题的回答。

我不相信打卡签到能带来成长，不相信看完视频等于掌握技能，不相信证书是能力的证明。我相信：

**真实的作品提交 → 严格的能力评估 → 可见的成长反馈 → 持续的正循环**

这就是 Odyssey 的全部。它不是游戏，但比游戏更让人上瘾——因为你建造的每一座建筑，都是你真实能力的证明。

如果你也厌倦了"学了很多但什么都不会"的焦虑，欢迎来到 Odyssey，建造属于你的能力文明。

---

*Odyssey — 你的能力，值得一座文明。*
